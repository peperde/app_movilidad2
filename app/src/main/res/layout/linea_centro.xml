<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fondo">

    <!-- Logo arriba -->
    <ImageView
        android:id="@+id/yallego"
        android:layout_width="110dp"
        android:layout_height="50dp"
        android:layout_marginTop="36dp"
        android:scaleType="centerInside"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/logo_yallegova1" />

    <ImageView
        android:id="@+id/logocentro"
        app:srcCompat="@drawable/logo_vancentro"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_marginStart="32dp"
        android:layout_marginBottom="404dp"
        android:background="@android:color/transparent"
        android:scaleType="fitCenter"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        />

    <!-- Fragmento del mapa -->
    <fragment
        android:id="@+id/mapFragmentLinea1"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="208dp"
        android:layout_height="334dp"
        android:layout_marginEnd="24dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/yallego" />

    <!-- Panel lateral: estado de ocupación -->

    <!-- Panel inferior: tiempo de llegada -->
    <TextView
        android:id="@+id/panel_estado"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="88dp"
        android:background="@color/black"
        android:padding="8dp"
        android:text="Disponible"
        android:textAlignment="center"
        android:textColor="@android:color/white"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/mapFragmentLinea1" />

    <!-- Botón regresar -->

    <TextView
        android:id="@+id/tiempoLlegada"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@color/naranja"
        android:padding="12dp"
        android:text="Faltan +10 min"
        android:textAlignment="center"
        android:textColor="@android:color/white"
        app:layout_constraintEnd_toEndOf="@id/mapFragmentLinea1"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="@id/mapFragmentLinea1"
        app:layout_constraintTop_toBottomOf="@id/mapFragmentLinea1" />

    <Button
        android:id="@+id/regresar_pagprincipal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="152dp"
        android:layout_marginBottom="44dp"
        android:text="Regresar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>